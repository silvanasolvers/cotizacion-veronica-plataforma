# Cotización Verónica Restrepo — Fase 2

Explorador web de la propuesta del ecosistema fractional de aprendizaje, comunidad y oportunidades.

```bash
npm install
npm run build
npm start
```

El servidor expone `GET /health` y sirve la aplicación en el puerto definido por `PORT`.
